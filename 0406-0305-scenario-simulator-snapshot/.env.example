DATABASE_URL=postgres://alimurtaza@127.0.0.1:5432/workshop
PORT=3000
FACILITATOR_SECRET=dev-secret
CONTENT_DIR=./content

# Railway (do this in the dashboard / CLI, not by committing secrets):
# 1. Add an "app" service in project the-practice-labs (Postgres already exists).
# 2. Set root directory to 0406-0305-scenario-simulator-snapshot, or deploy with
#    railway.toml at the repo root (Dockerfile path already set).
# 3. Variables on the app service: DATABASE_URL=${{Postgres.DATABASE_URL}}
#    FACILITATOR_SECRET=<share this in the facilitator URL>
# 4. Generate a public domain. Participant join is /. Facilitator is /facilitate/<secret>
# 5. Postgres plugin currently shows a failed deploy — fix that volume before Wednesday.
